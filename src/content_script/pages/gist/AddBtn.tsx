import React, { useEffect } from 'react';
import Btn from '@/content_script/components/Btn';

import '@/utils/leancloud'

const AddBtn = () => {
  useEffect(() => {
    async function fetchL() {

    }
  })
  return <Btn text='+' className="btn btn-sm" />
}

export default AddBtn;